# HTTP UI
PORT=5888
HTTP_THREADS=16

# WebSocket-to-VNC proxy
# Keep this on loopback unless access is protected by a firewall or reverse proxy.
WS_PROXY_HOST=127.0.0.1
WS_PROXY_PORT=6080

# Replace with a long, random value before deployment.
SECRET_KEY=replace-with-a-long-random-secret
